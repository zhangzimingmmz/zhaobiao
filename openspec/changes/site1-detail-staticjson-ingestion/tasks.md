## 1. site1 详情抓取

- [x] 1.1 梳理 `site1` 三类公告详情页的可解析节点，并在代码中抽成独立 helper。
- [x] 1.2 为 `crawler/site1` 增加详情页请求与重试逻辑，解析正文 HTML、`originurl`、关联编号等字段。
- [x] 1.3 将列表记录与详情记录合并为统一落库 payload，`raw_json` 改为 `_list` + `_detail` 组合结构。

## 2. 落库与读取

- [x] 2.1 调整 site1 落库策略，让 `content` / `origin_url` 优先使用详情层数据，缺失时回退列表层。
- [x] 2.2 新增 site1 历史详情回填任务，对已有记录按 merge 方式补抓详情页。
- [x] 2.3 更新详情接口与正文渲染逻辑，让 site1 在存在详情 HTML 时优先按 HTML 渲染。

## 3. 验证与文档

- [x] 3.1 补充 crawler 与 API 测试，覆盖至少一条 `002001009`、一条 `002001001`、一条 `002002001` 的列表+详情合并场景。
- [x] 3.2 用真实样本验证 site1 招标计划与招标公告的展示效果明显优于列表压平文本。
- [x] 3.3 更新采集设计/API 文档，明确 site1 已采用详情页落库。
