## 1. 公网入口与部署配置

- [x] 1.1 为招标系统补充可落地的 Traefik 动态路由模板/文档，明确 `admin-zhaobiao` 与 `api-zhaobiao` 两个域名分别转发到 `100.64.0.5:8091`、`100.64.0.5:8000`
- [x] 1.2 扩展后端部署示例配置与文档，加入公网域名、前端公网 API base 以及部署后的验证命令
- [x] 1.3 在 `100.64.0.7` 上加载招标系统 Traefik 动态配置并验证两个公网域名都能通过 HTTPS 命中对应服务

## 2. 运营平台公网 API 接入

- [x] 2.1 调整 `admin-frontend` 的构建配置，使生产-like 构建使用可配置的公网 HTTPS API base，而不是 `100.64.0.5`
- [x] 2.2 重新部署运营平台前端并验证页面资源、SPA 路由与 API 请求都通过公网域名工作

## 3. 小程序公网 API 域名

- [x] 3.1 将小程序 API base 配置改为支持 `TARO_APP_API_BASE` 注入，并保留 `localhost` 作为本地开发兜底
- [x] 3.2 更新小程序文档，说明公网 API 域名配置、微信 request 合法域名要求与构建方式
- [x] 3.3 生成生产-like 小程序构建配置并验证请求 URL 指向公网 HTTPS API 域名

## 4. 端到端验证与收口

- [x] 4.1 验证公网 API 域名下的 `/openapi.json` 与核心 `/api/*` 接口返回正常
- [x] 4.2 验证运营平台与小程序均不再依赖 `100.64.0.5` 或 `localhost` 作为生产-like API 地址
