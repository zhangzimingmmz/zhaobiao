## 1. 服务端正文渲染策略

- [x] 1.1 为 notices 详情读取链路引入独立的正文格式化模块，并按 `site1_sc_ggzyjy` / `site2_ccgp_sichuan` 分派策略。
- [x] 1.2 为 `site2` 实现 HTML 轻清洗与统一样式规则，保留段落、表格、列表、图片和链接等必要结构。
- [x] 1.3 为 `site1` 实现文本结构化分段规则，覆盖编号前缀、章节标记和高频字段边界。
- [x] 1.4 为异常正文补充统一降级路径，确保始终返回可读段落或空内容。

## 2. 详情接口与小程序消费

- [x] 2.1 更新 `GET /api/detail/bid/:id` 和 `GET /api/detail/info/:id`，让 `content` 返回来源感知后的渲染结果。
- [x] 2.2 确认小程序招投标详情页与信息详情页只消费服务端渲染结果，不在客户端再做站点特有解析。

## 3. 验证与文档

- [x] 3.1 补充服务端测试，覆盖 site1 文本重排、site2 HTML 清洗、异常正文降级三类场景。
- [x] 3.2 以真实样本验证至少一条 site1 招标计划、一条 site1 招标公告、一条 site2 采购公告和一条 site2 采购意向的展示效果。
- [x] 3.3 更新接口文档或相关说明，明确 detail 接口中的 `content` 为已渲染正文而非原始存储内容。
