## 1. Favorites tab surface

- [x] 1.1 Define the favorites tab layout, title treatment, and type-switching model.
- [x] 1.2 Define guest, empty, and populated favorites states under the first-level tab shell.
- [x] 1.3 Remove older assumptions that favorites is mainly reached through a home or profile shortcut.

## 2. Profile tab surface

- [x] 2.1 Redesign the profile tab around guest and authenticated account states.
- [x] 2.2 Define how pending, approved, and rejected certification states appear inside the profile surface.
- [x] 2.3 Remove duplicated “favorites as primary CTA” patterns from profile.

## 3. Shared favorites model

- [x] 3.1 Define the normalized local favorites record shape shared across notice types.
- [x] 3.2 Persist and restore the user’s selected favorites type.
- [x] 3.3 Define how favorites-tab refresh and page-local favorite actions stay in sync through the shared storage model.
