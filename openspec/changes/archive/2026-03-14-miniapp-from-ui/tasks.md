## 1. 项目初始化（miniapp-project）

- [x] 1.1 在 `miniapp/` 下使用 Taro 脚手架初始化 React 项目，选择微信小程序为编译目标
- [x] 1.2 配置 Taro 的 config（如 `config/index.ts`），设置 outputRoot 为 `dist`，确保产出目录可被微信开发者工具打开
- [x] 1.3 在 `miniapp/package.json` 中配置 `dev:weapp` 与 `build:weapp` 脚本，并在 `miniapp/README.md` 中说明安装依赖、构建/开发、以及用微信开发者工具打开 `miniapp/dist` 的步骤
- [x] 1.4 在产出目录中提供或生成 `project.config.json`（含 appid 等），确保微信开发者工具能正确识别项目

## 2. 设计系统与基础样式（miniapp-ui）

- [x] 2.1 在 `miniapp` 内建立设计 token（主色 #1677FF、中性色、功能色、微信绿等），与 `ui/DESIGN_SYSTEM.md` 一致，并在 WXSS 或全局样式中使用
- [x] 2.2 实现基础布局与字号、圆角等通用样式类，供各页面与组件复用

## 3. 核心组件（miniapp-ui）

- [x] 3.1 实现顶部栏组件（TopBar）：支持标题、返回、个人中心/收藏/分享等插槽，与 ui 行为一致
- [x] 3.2 实现一级 Tab（PrimaryTabs）与二级 Tab（SecondaryTabs），支持选中态与切换回调
- [x] 3.3 实现筛选栏（FilterBar）与筛选弹层（FilterSheet），支持按当前菜单展示不同筛选项（发布时间、地区、采购方式等）
- [x] 3.4 实现招投标卡片（BidCard）与信息展示卡片（InfoCard），展示类型、标题、时间等关键信息
- [x] 3.5 实现底部导航（BottomNav）：首页 / 我的，与 TabBar 或自定义组件一致
- [x] 3.6 实现空状态组件（EmptyState），在列表无数据时展示

## 4. 页面与路由（miniapp-pages）

- [x] 4.1 配置小程序 app 与页面路由：首页、招投标详情、信息展示详情、登录、注册、审核状态、我的
- [x] 4.2 实现首页：集成 TopBar、一级/二级 Tab、FilterBar、列表区与 BottomNav，完成菜单与筛选联动
- [x] 4.3 实现招投标详情页：标题卡片、项目信息、重要时间、公告正文、收藏按钮、查看原文
- [x] 4.4 实现信息展示详情页：文章头、摘要、正文、公众号原文按钮（微信绿）
- [x] 4.5 实现登录页：Logo、手机号、验证码、获取验证码倒计时、登录按钮、企业注册入口
- [x] 4.6 实现注册/企业认证页：企业名称、信用代码、联系人、电话、营业执照上传、提交
- [x] 4.7 实现审核状态页：审核中/通过/未通过三种状态的展示与入口
- [x] 4.8 实现我的页面：企业信息卡片、我的收藏、企业信息、设置、联系客服、退出登录、版本信息及 BottomNav

## 5. 接口与数据层（miniapp-data）

- [x] 5.1 封装请求基础配置（base URL、超时、鉴权头等），与 `接口文档.md` 约定一致
- [x] 5.2 实现列表接口调用（招投标列表、信息展示列表），支持分页与筛选参数
- [x] 5.3 实现详情接口调用（招投标详情、信息展示详情）
- [x] 5.4 实现登录、验证码、注册接口封装及调用，与接口文档一致
- [x] 5.5 实现收藏相关接口（若接口文档中有定义）及详情页「原文」链接跳转

## 6. 联调与收尾

- [x] 6.1 将首页、详情页、登录注册审核流程与 API 层对接，使用 Mock 或真实后端验证列表与详情展示
- [x] 6.2 在微信开发者工具中打开 `miniapp/dist`，验证 7 个页面可访问、导航与筛选联动正常，样式与设计系统一致
- [x] 6.3 补充 README 中关于环境变量（如 API base URL）与本地/真机调试的说明（若需要）
