## 1. Tool Area Hierarchy

- [x] 1.1 调整频道页工具区整体层级，让搜索框成为主工具区的主视觉，同时保留二级 tab 和筛选按钮作为次级控件。
- [x] 1.2 调整 `SecondaryTabs`、`FilterBar` 和频道页工具区外层样式，确保控件的大小、间距和背景层级符合“搜索优先、筛选次之”的阅读顺序。

## 2. Scan-First List Cards

- [x] 2.1 调整 `BidCard` 的标题、标签、来源和发布时间的视觉层级，使标题成为唯一强层，辅助信息整体进一步弱化。
- [x] 2.2 收紧 `BidCard` 的元信息组织方式和垂直节奏，减少重复感并提升一屏扫读效率，同时保持现有字段与交互不变。
- [x] 2.3 检查收藏页等复用 `BidCard` 的列表场景，确认新的扫读优先样式不会破坏现有页面可读性。

## 3. Validation

- [x] 3.1 重新编译小程序，确认本次纯 UI 调整不会引入构建错误。
- [ ] 3.2 在微信开发者工具中核对频道页和收藏页的最新构建，确认页面已更接近“搜索优先、列表好扫、辅助信息弱化”的目标，而没有引入新的功能行为变化。
