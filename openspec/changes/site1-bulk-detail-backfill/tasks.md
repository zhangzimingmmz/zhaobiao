## 1. 批量回填脚本

- [x] 1.1 为 `detail_backfill` 增加默认三分类顺序、批次大小、请求间隔与失败阈值参数。
- [x] 1.2 让批量回填只补齐缺少 `_detail` 的记录，并输出每分类/每批次统计日志。
- [x] 1.3 增加 dry-run 模式，支持正式执行前的只读验证。

## 2. 测试与文档

- [x] 2.1 补充测试，覆盖默认分类顺序、dry-run 不写库、分批统计行为。
- [x] 2.2 更新采集与运维文档，明确三分类批量回填的推荐执行顺序和命令。
